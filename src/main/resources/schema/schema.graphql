type Query {
    allHellos: [Hello]
    oneHello: Hello
    monTest(input: String!): String
}

type Hello {
    text: String!
    randomNumber: Int
}

# Optional
# schema {
#     query: Query
#     mutation: Mutation
# }